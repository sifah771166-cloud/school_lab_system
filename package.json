{
  "name": "school-lab-system",
  "version": "1.0.0",
  "description": "School Laboratory Management System",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm --prefix backend run dev\" \"npm --prefix frontend run dev\"",
    "build": "npm --prefix backend ci && npm --prefix backend run build && npm --prefix frontend ci && npm --prefix frontend run build",
    "start": "npm --prefix backend start",
    "install-all": "npm ci && npm --prefix backend ci && npm --prefix frontend ci"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "dependencies": {
    "@prisma/client": "^5.22.0",
    "prisma": "^5.22.0"
  }
}
